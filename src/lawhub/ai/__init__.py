"""Forward-looking AI layer.

`deal_flow` ships today (structured domain knowledge). `retrieval` is a
documented interface stub for the RAG / grounded-answer layer described in
docs/ROADMAP.md — kept dependency-free so the repo stays runnable.
"""
